context.modules = [
    { name = libpipewire-module-filter-chain
        args = {
            node.name        = "bluetooth_tompito"
            node.description = "EQ: magas hang levágás 2"
            media.name       = "EQ: magas hang levágás 2"
            
            filter.graph = {
                nodes = [
                    {
                        type    = builtin
                        name    = eq_magas_vagas
                        label   = bq_highshelf
                        control = { "Freq" = 4000.0 "Q" = 1.0 "Gain" = -6.0 }
                    }
                ]
                # SEMMI LINKS! A PipeWire automatikusan huzaloz, ha a capture/playback jól van beállítva.
            }
            
            capture.props = {
                node.name    = "effect_input.eq"
                media.class  = "Audio/Sink"
                audio.channels = 2
                audio.position = [ FL FR ]
                node.passive = true
                node.always-process = false
                node.suspend-on-idle = 3
                session.suspend-on-idle = true
            }
            playback.props = {
                node.name    = "effect_output.eq"
                audio.channels = 2
                audio.position = [ FL FR ]
                node.passive = true
                node.always-process = false
                node.suspend-on-idle = 3
                session.suspend-on-idle = true
            }
        }
    }
]
